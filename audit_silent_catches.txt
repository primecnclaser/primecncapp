SILENT / EMPTY CATCH BLOCK AUDIT — index.html
Generated: 2026-07-02
Scope: every try/catch in index.html was located and its catch body parsed
(brace/string-aware scan, not naive text matching) to check whether it logs
the actual caught Error object. READ ONLY — no code changes made.

════════════════════════════════════════════════════════════════════════
METHODOLOGY & TOTALS
════════════════════════════════════════════════════════════════════════

330 total try/catch blocks exist in index.html. Each catch body was
classified as follows:

  87  EMPTY               catch(x){} — completely empty body
   3  EMPTY_RETURN_ONLY   catch(x){ return; } / { return null; } — no logging
  66  NO_LOG_NO_REF       body has code (toast/DOM/fallback) but never calls
                          console.* with the caught error variable — the real
                          Error object/stack trace is still never recorded
 144  WARN_WITH_VAR       console.warn(...) IS called AND does pass the caught
                          error variable as an argument — NOT silent, excluded
  30  HAS_LOGGING         console.error(...) called with the error variable —
                          correct pattern, excluded
 ---
 330  total

NOTE on the requested "console.warn(...) but doesn't log the actual error
object" pattern (item 2 in the request): searching the whole file found
ZERO cases of this specific pattern — every single console.warn() call in a
catch block in this codebase does pass the caught error/exception object as
an argument (e.g. `console.warn('X failed:', e)`). That specific failure mode
does not currently exist here. What DOES exist at meaningful scale is the
broader problem: catch blocks that log NOTHING about the error at all (87
fully empty + 3 no-op-return + 66 "does something but never logs the error"
= 156 blocks, 47% of all 330 catch blocks in the file).

This report covers all 156 of those blocks, grouped by risk level based on
what the corresponding try block was attempting to do:
  HIGH   = try block does a Supabase write (insert/update/upsert/delete/rpc)
  MEDIUM = try block reads/fetches data (select/fetch/JSON.parse/localStorage)
  LOW    = try block only touches the DOM/UI/local formatting

  HIGH:   40
  MEDIUM: 64
  LOW:    52

════════════════════════════════════════════════════════════════════════
HIGH RISK  (40 catch blocks)
════════════════════════════════════════════════════════════════════════

[1] Line 5282  (try starts line 5274)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: await sbHO.from('branch_sync_log').insert({  |  branch_id:     'ba08fc3a-5b6a-473a-8e69-1867fa54d280',
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[2] Line 8879  (try starts line 8875)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Save failed: ' + e.message); return;
      }
    Try block does: if (typeof sb !== 'undefined' && sb)  |  await sb.from('excel_invoices').update({ total_bill: amt, remaining: row.remaining }).eq('order_num', _rawOrde…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[3] Line 8913  (try starts line 8884)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Save failed: ' + e.message); return;
      }
    Try block does: if (typeof sb !== 'undefined' && sb) {  |  await sb.from('excel_invoices').update({ discount: amt, remaining: row.remaining }).eq('order_num', _rawOrderN…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[4] Line 8947  (try starts line 8918)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Save failed: ' + e.message); return;
      }
    Try block does: if (typeof sb !== 'undefined' && sb) {  |  await sb.from('excel_invoices').update({ bad_debt: amt, remaining: row.remaining }).eq('order_num', _rawOrderN…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[5] Line 9044  (try starts line 9015)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Received save failed: ' + e.message);
      }
    Try block does: await sb.from('excel_invoices').update({  |  received: rvAmt,
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[6] Line 9087  (try starts line 9087)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: await sb.from('excel_invoices').update({ received_entries: JSON.stringify(row.received_entries) }).eq('order_n…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[7] Line 13317  (try starts line 13188)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Save failed: ' + (e.message || e));
      }
    Try block does: var newRow = {  |  customer_name:    name,
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[8] Line 14314  (try starts line 14303)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('💾 Shop name saved locally (cloud sync failed)');
      }
    Try block does: var rows = [  |  { key: 'shop_display_name', value: name,  updated_at: new Date().toISOString() },
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[9] Line 15288  (try starts line 15242)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        status.style.display = '';
        status.style.background = '#fef2f2'; status.style.color = '#dc2626';
        status.textContent = '⚠️ Saved locally. Cloud sync failed: ' + (e.message || e);
      }
    Try block does: if (vals.logo && vals.logo.startsWith('data:')) {  |  try {
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[10] Line 15837  (try starts line 15798)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        listEl.innerHTML = '<div style="text-align:center;padding:30px;color:#dc2626;font-family:Montserrat,sans-serif;">❌ Failed to load orders: ' + (e.message||e) + '</div>';
      }
    Try block does: var _now = Date.now();  |  var _rpcRes = await sb.rpc('get_latest_production_orders', { p_limit: 30 });
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[11] Line 15948  (try starts line 15920)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed to update status: ' + (e.message||e));
      }
    Try block does: var table = type === 'new' ? 'invoices' : 'excel_invoices';  |  var idCol = type === 'new' ? 'id' : 'id';
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[12] Line 16499  (try starts line 16493)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: await sb.from('customer_payments').delete().eq('id', paymentId);  |  if (typeof _rpInvalidateSummaryCache === 'function') _rpInvalidateSummaryCache();
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[13] Line 16517  (try starts line 16510)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: await sb.from('customer_payments').update({ amount: newAmt }).eq('id', paymentId);  |  if (typeof _rpInvalidateSummaryCache === 'function') _rpInvalidateSummaryCache();
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[14] Line 16565  (try starts line 16549)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: var _rawNum = String(orderNum).replace(/^INV-/i,'');  |  var row = (_excelDBCache||[]).find(function(r){ return String(r.order_num).replace(/^INV-/i,'') === _rawNum; }…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[15] Line 16585  (try starts line 16573)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: if (typeof sb === 'undefined' || !sb) throw new Error('Not connected');  |  var _rawNum = String(orderNum).replace(/^INV-/i,'');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[16] Line 16629  (try starts line 16613)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: var _rawNum = String(orderNum).replace(/^INV-/i,'');  |  var row = (_excelDBCache||[]).find(function(r){ return String(r.order_num).replace(/^INV-/i,'') === _rawNum; }…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[17] Line 16659  (try starts line 16648)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: if (typeof sb === 'undefined' || !sb) throw new Error('Not connected');  |  var row = (_excelDBCache||[]).find(function(r){ return String(r.order_num).replace(/^INV-/i,'') === _rawNum; }…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[18] Line 16679  (try starts line 16667)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed: ' + (e.message||e));
      }
    Try block does: if (typeof sb === 'undefined' || !sb) throw new Error('Not connected');  |  var _rawNum = String(orderNum).replace(/^INV-/i,'');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[19] Line 19635  (try starts line 19630)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        failed += batch.length;
        logEl.innerHTML += `<div style="color:#dc2626;">❌ Failed batch at ${monthLabel}: ${e.message}</div>`;
      }
    Try block does: const { error } = await sb.from('finance_ledger').insert(batch);  |  if (error) throw error;
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[20] Line 19804  (try starts line 19804)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _finDeleteQueue.push(editLocalId); _saveFinDelete();
      }
    Try block does: await sb.from('finance_ledger').delete().eq('ref_id', editLocalId).eq('source','manual');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[21] Line 19818  (try starts line 19818)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _finDeleteQueue.push(oldId); _saveFinDelete();
      }
    Try block does: await sb.from('finance_ledger').delete().eq('ref_id', oldId).eq('source','manual');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[22] Line 19972  (try starts line 19970)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        // Queue for later
        _finDeleteQueue.push(localId);
        _saveFinDelete();
      }
    Try block does: await sb.from('finance_ledger').delete().eq('ref_id', localId).eq('source', 'manual');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[23] Line 20452  (try starts line 20452)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _finDeleteQueue.push(id); _saveFinDelete();
      }
    Try block does: await sb.from('finance_ledger').delete().eq('ref_id', id).eq('source','manual');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[24] Line 20459  (try starts line 20459)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Error: ' + e.message); return;
      }
    Try block does: await sb.from('finance_ledger').delete().eq('id', id); toast('🗑️ Entry deleted');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[25] Line 20479  (try starts line 20479)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _finDeleteQueue.push(id); _saveFinDelete();
      }
    Try block does: await sb.from('finance_ledger').delete().eq('ref_id', id).eq('source','manual');
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[26] Line 20484  (try starts line 20484)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('❌ Error: ' + e.message); return;
      }
    Try block does: await sb.from('finance_ledger').delete().eq('id', id);
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[27] Line 25692  (try starts line 25654)  —  var: err
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(err) {
        if (btn) { btn.textContent = '▶ Apply Layout'; btn.disabled = false; }
        _showStatus(null, '&#10003; Layout applied (cloud error: ' + (err.message||err) + ')', '#fff7ed', '#ea580c');
      }
    Try block does: if (vals.logo && vals.logo.startsWith('data:')) {  |  try {
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[28] Line 26499  (try starts line 26495)  —  var: err
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(err) {
        _showStatus(null, '&#10003; Layout saved locally. Cloud error: '+(err.message||err), '#fff7ed', '#ea580c');
      }
    Try block does: var res = await sb.from('shop_settings').upsert(rows, {onConflict:'key'});  |  if (res.error) throw res.error;
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[29] Line 27080  (try starts line 27073)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if (!window.sb) return;  |  var sess = _authGetSession();
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[30] Line 27208  (try starts line 27190)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(err){err.textContent='⚠️ '+(e.message||'');err.style.display='';}
        if(btn){btn.disabled=false;btn.textContent='Set New Password';}
      }
    Try block does: var hash = await hashPassword(pw1);  |  var res = await window.sb.from('app_users')
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[31] Line 27601  (try starts line 27590)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if (err) { err.textContent = '⚠️ Could not save. Try again.'; err.style.display = ''; }
        if (btn) { btn.disabled = false; btn.textContent = 'Save & Continue'; }
      }
    Try block does: if (window.sb) {  |  await window.sb.from('app_users').update({
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[32] Line 27677  (try starts line 27647)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(err){err.textContent='⚠️ '+(e.message||'Error');err.style.display='';}
        if(btn){btn.disabled=false;btn.textContent='Create Admin Account';}
      }
    Try block does: var hash = await hashPassword(password);  |  var res = await window.sb.from('app_users').insert({
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[33] Line 27733  (try starts line 27707)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(err){err.textContent='⚠️ '+(e.message||'');err.style.display='';}
        if(btn){btn.disabled=false;btn.textContent='Set Password & Continue';}
      }
    Try block does: var hash = await hashPassword(newPw);  |  var res = await window.sb.from('app_users').update({
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[34] Line 27962  (try starts line 27948)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(typeof window.showPwError==='function') window.showPwError('⚠️ Error: '+(e.message||'Check connection'));
        if(btn){btn.disabled=false;btn.textContent='🔑 Update Password';}
      }
    Try block does: var currHash = await hashPassword(currentPw);  |  var chk = await window.sb.from('app_users').select('id').eq('id',sess.id).eq('password_hash',currHash).single(…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[35] Line 28055  (try starts line 28041)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _showMsg('⚠️ '+(e.message||'Error. Try again.'), false);
        if(btn){ btn.disabled=false; btn.textContent='Save'; }
      }
    Try block does: var res = await window.sb.from('app_users').update({  |  email: email||null, phone: phone||null,
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[36] Line 28413  (try starts line 28404)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _msg('⚠️ Error: '+(e.message||'Check connection'),false);
      }
    Try block does: var currHash = await hashPassword(currPw);  |  var chk = await window.sb.from('app_users').select('id').eq('id',sess.id).eq('password_hash',currHash).single(…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[37] Line 28515  (try starts line 28491)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(err){err.textContent='⚠️ '+(e.message||'Error');err.style.display='';}
      }
    Try block does: var hash = await hashPassword(pw);  |  var perms = {};
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[38] Line 28627  (try starts line 28607)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var editEmail=((document.getElementById('editEmail')||{}).value||'').trim();  |  var editPhone=((document.getElementById('editPhone')||{}).value||'').trim();
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[39] Line 28672  (try starts line 28662)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(err){err.textContent='⚠️ Error';err.style.display='';}
      }
    Try block does: var hash=await hashPassword(p1);  |  var res=await window.sb.from('app_users').update({password_hash:hash,force_pw_change:true}).eq('id',_authEditT…
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
[40] Line 28692  (try starts line 28687)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(typeof window.toast==='function')window.toast('❌ Delete failed.');
      }
    Try block does: await window.sb.from('app_users').delete().eq('id',userId);  |  _authLog('user_deleted','Deleted user: '+username);
    Risk: HIGH — try block performs a Supabase insert/update/upsert/delete/rpc — a failed write is silently lost
════════════════════════════════════════════════════════════════════════
MEDIUM RISK  (64 catch blocks)
════════════════════════════════════════════════════════════════════════

[1] Line 4839  (try starts line 4839)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _s=JSON.parse(localStorage.getItem('inv_hdr_el_styles')||'{}'); if(_s.urdu && !_s.urdu.font) _s.urdu.font=…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[2] Line 5595  (try starts line 5585)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if (window._sbConnectionOk) {
        window._sbConnectionOk = false;
        if (typeof toast === 'function') toast('⚠️ Cloud connection lost — working offline');
        }
      }
    Try block does: if (typeof sb === 'undefined' || !sb) return;  |  var _r = await sb.from('app_users').select('id').limit(1);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[3] Line 5713  (try starts line 5713)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: _sess = JSON.parse(sessionStorage.getItem('_authSession') || 'null');
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[4] Line 7133  (try starts line 7127)  —  var: _e2
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_e2) {}
    Try block does: var _lds2 = JSON.parse(localStorage.getItem("drafts_db") || "[]");  |  _lds2.forEach(function(d) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[5] Line 7956  (try starts line 7952)  —  var: _cpE2
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_cpE2) {}
    Try block does: var _cpP = JSON.parse(localStorage.getItem('cp_pending_sync') || '[]');  |  _cpP = _cpP.filter(function(e) { return e.invoice_ref !== data.invNum; });
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[6] Line 9377  (try starts line 9366)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        return 0;
      }
    Try block does: var q = sb.from('excel_invoices').select('id', { count: 'exact', head: true });  |  if (mode === 'day' && dateV) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[7] Line 9826  (try starts line 9823)  —  var: _ce
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_ce) {}
    Try block does: var cached = JSON.parse(localStorage.getItem('cash_holders_cache') || 'null');  |  if (cached) { names = cached.names || names; entries2 = cached.entries2 || []; entries3 = cached.entries3 || […
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[8] Line 10117  (try starts line 10117)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: c = JSON.parse(localStorage.getItem('summary_cache') || 'null');
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[9] Line 10171  (try starts line 10168)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: const s = localStorage.getItem('inv_partners');  |  if (s) return JSON.parse(s);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[10] Line 11243  (try starts line 11243)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        r.received_entries = [];
      }
    Try block does: r.received_entries = JSON.parse(r.received_entries);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[11] Line 11706  (try starts line 11706)  —  var: e
    Pattern: NO-OP — catch(x){ return; } (no logging, just returns)
    Catch block:
      catch(e) {
        return;
      }
    Try block does: info = JSON.parse(decodeURIComponent(encodedKey));
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[12] Line 12210  (try starts line 12200)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var allRecs = [], from = 0, batch = 1000, done = false;  |  while (!done) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[13] Line 14144  (try starts line 14131)  —  var: e2
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e2) {}
    Try block does: var ledgerRaw = localStorage.getItem('pcnc_fin_ledger');  |  if (ledgerRaw) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[14] Line 14237  (try starts line 14224)  —  var: e2
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e2) {}
    Try block does: var ledgerRaw = localStorage.getItem('pcnc_fin_ledger');  |  if (ledgerRaw) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[15] Line 14598  (try starts line 14587)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var s=localStorage.getItem('inv_hdr_el_styles');  |  if(s) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[16] Line 14606  (try starts line 14606)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var c=localStorage.getItem('inv_hdr_cols'); if(c) return JSON.parse(c);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[17] Line 14612  (try starts line 14612)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var o=localStorage.getItem('inv_hdr_order'); if(o){ var p=JSON.parse(o); if(p && p.length>0) return p; }
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[18] Line 14617  (try starts line 14617)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var h=localStorage.getItem('inv_hdr_hidden'); if(h) return JSON.parse(h);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[19] Line 14914  (try starts line 14903)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _es=localStorage.getItem('inv_hdr_el_styles');  |  if(_es) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[20] Line 14915  (try starts line 14915)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _ec=localStorage.getItem('inv_hdr_cols');       if(_ec) _hdrCols=JSON.parse(_ec);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[21] Line 14916  (try starts line 14916)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _eo=localStorage.getItem('inv_hdr_order'); if(_eo){ var _eop=JSON.parse(_eo); if(_eop && _eop.length>0) _h…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[22] Line 14917  (try starts line 14917)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _eh=localStorage.getItem('inv_hdr_hidden');     if(_eh) _hdrHidden=JSON.parse(_eh);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[23] Line 14978  (try starts line 14978)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _p=JSON.parse(_v); if(_p && _p.length>0) localStorage.setItem('inv_hdr_order',_v);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[24] Line 14999  (try starts line 14999)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _pp=JSON.parse(map['inv_partners']); if(Array.isArray(_pp)){localStorage.setItem('inv_partners',JSON.strin…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[25] Line 15000  (try starts line 15000)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _fc=JSON.parse(map['pcnc_fin_categories']); if(_fc&&typeof _fc==='object'){FIN_CATEGORIES=_fc;localStorage…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[26] Line 15003  (try starts line 15003)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _dr=JSON.parse(map['inv_drafts']); if(Array.isArray(_dr)){ draftsDB=_dr; localStorage.setItem('inv_drafts'…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[27] Line 15028  (try starts line 15018)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if(map.hdr_el_styles) {  |  var _cloudStyles = JSON.parse(map.hdr_el_styles);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[28] Line 15029  (try starts line 15029)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if(map.hdr_cols)      _hdrCols     = JSON.parse(map.hdr_cols);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[29] Line 15030  (try starts line 15030)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if(map.hdr_order)     _hdrOrder    = JSON.parse(map.hdr_order);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[30] Line 15031  (try starts line 15031)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if(map.hdr_hidden)    _hdrHidden   = JSON.parse(map.hdr_hidden);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[31] Line 15032  (try starts line 15032)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if(map.drag_free_pos && !_dragElStylesTemp) _dragFreePos = JSON.parse(map.drag_free_pos);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[32] Line 15552  (try starts line 15552)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _p=JSON.parse(_v); if(_p && _p.length>0) localStorage.setItem('inv_hdr_order',_v);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[33] Line 15573  (try starts line 15573)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _pp2=JSON.parse(map['inv_partners']); if(Array.isArray(_pp2)){localStorage.setItem('inv_partners',JSON.str…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[34] Line 15574  (try starts line 15574)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _fc2=JSON.parse(map['pcnc_fin_categories']); if(_fc2&&typeof _fc2==='object'){FIN_CATEGORIES=_fc2;localSto…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[35] Line 15577  (try starts line 15577)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _dr2=JSON.parse(map['inv_drafts']); if(Array.isArray(_dr2)){ draftsDB=_dr2; localStorage.setItem('inv_draf…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[36] Line 15647  (try starts line 15647)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        r.received_entries = [];
      }
    Try block does: r.received_entries = JSON.parse(r.received_entries);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[37] Line 16979  (try starts line 16963)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var res = await sb.from('invoices')  |  .select('cust_name,phone,inv_num,date,total_bill,received,discount,remaining,bad_debt')
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[38] Line 16998  (try starts line 16982)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var res2 = await sb.from('excel_invoices')  |  .select('customer_name,phone,order_num,date,total_bill,received,discount,remaining')
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[39] Line 18089  (try starts line 18086)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: const saved = localStorage.getItem('pcnc_fin_categories');  |  if (saved) return JSON.parse(saved);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[40] Line 19054  (try starts line 19045)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: const { data } = await sb.from('finance_ledger').select('amount,note,entry_date').eq('entry_date',date).eq('en…  |  if (data && data.length) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[41] Line 19082  (try starts line 19058)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: const monthStart = ym + '-01';  |  const monthEnd   = ym + '-31';
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[42] Line 19461  (try starts line 19371)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        statusEl.textContent = '❌ Error: ' + e.message;
        logEl.innerHTML += `<div style="color:#dc2626;">Error: ${e.message}</div>`;
      }
    Try block does: statusEl.textContent = 'Step 1/3: Removing 2025 Annual Summary Import duplicates...';  |  logEl.innerHTML += '<div>🔍 Fetching "2025 Annual Summary Import" entries...</div>';
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[43] Line 19491  (try starts line 19488)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        return [];
      }
    Try block does: const raw = localStorage.getItem(FIN_CACHE_KEY);  |  return raw ? JSON.parse(raw) : [];
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[44] Line 20072  (try starts line 20069)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: const { data } = await sb.from('finance_ledger').select('entry_date').order('entry_date', { ascending: false }…  |  supaYears = (data || []).map(r => (r.entry_date || '').slice(0,4)).filter(Boolean);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[45] Line 25430  (try starts line 25416)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var v = localStorage.getItem('inv_drag_free_pos');  |  if (v) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[46] Line 26119  (try starts line 26119)  —  var: e
    Pattern: NO-OP — catch(x){ return; } (no logging, just returns)
    Catch block:
      catch(e) {
        return;
      }
    Try block does: tmpl=JSON.parse(raw);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[47] Line 26132  (try starts line 26132)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: name=JSON.parse(raw).name||name;
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[48] Line 26147  (try starts line 26147)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if(raw)tmpl=JSON.parse(raw);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[49] Line 26193  (try starts line 26183)  —  var: ex
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(ex) {
        alert('⚠ Invalid JSON: '+ex.message);
      }
    Try block does: var data=JSON.parse(e.target.result);  |  if(!confirm('Import settings from "'+file.name+'"? This replaces current settings.')) { input.value=''; return…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[50] Line 26719  (try starts line 26706)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var raw = localStorage.getItem(FIN_CROSSCHECK_KEY);  |  if (!raw) { _finCCSetStatus('📭 No finance entries found', '#475569', '#f1f5f9', false); return; }
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[51] Line 26732  (try starts line 26732)  —  var: _e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(_e) {
        _offT='—';
      }
    Try block does: var _offR=JSON.parse(localStorage.getItem(FIN_CROSSCHECK_KEY)||'{}'); _offT=_offR.time?new Date(_offR.time).to…
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[52] Line 27024  (try starts line 27024)  —  var: e
    Pattern: NO-OP — catch(x){ return; } (no logging, just returns)
    Catch block:
      catch(e) {
        return null;
      }
    Try block does: return JSON.parse(localStorage.getItem(AUTH_KEY) || 'null');
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[53] Line 27171  (try starts line 27147)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(err){err.textContent='⚠️ '+(e.message||'Connection error');err.style.display='';}
        if(btn){btn.disabled=false;btn.textContent='Find My Account';}
      }
    Try block does: var res = await window.sb.from('app_users')  |  .select('id,username,display_name,role,email,phone')
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[54] Line 27295  (try starts line 27290)  —  var: ignore
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(ignore) {
        _authShowPanel('authSetupCard'); // table may not exist yet — show setup
      }
    Try block does: var result = await window.sb.from('app_users').select('id').limit(1);  |  if (!result.error && (!result.data || result.data.length === 0)) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[55] Line 27402  (try starts line 27402)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: _offAuth = JSON.parse(localStorage.getItem('_offlineAuth') || 'null');
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[56] Line 28087  (try starts line 28084)  —  var: _ce
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_ce) {}
    Try block does: var cached = JSON.parse(localStorage.getItem('cash_holders_cache') || 'null');  |  if (cached) { names = cached.names || names; entries2 = cached.entries2 || []; entries3 = cached.entries3 || […
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[57] Line 28093  (try starts line 28090)  —  var: _te
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_te) {}
    Try block does: var ciCache = JSON.parse(localStorage.getItem('summary_cache') || 'null');  |  if (ciCache && typeof ciCache.cashInHand === 'number') totalCash = ciCache.cashInHand;
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[58] Line 28158  (try starts line 28153)  —  var: _e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_e) {}
    Try block does: var cached = JSON.parse(localStorage.getItem('cash_holders_cache') || '{}');  |  cached.names = cached.names || {};
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[59] Line 28554  (try starts line 28525)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        container.innerHTML='<div style="color:#dc2626;">Error: '+_e(e.message||'')+'</div>';
      }
    Try block does: var res = await window.sb.from('app_users').select('*').order('created_at');  |  if(res.error){container.innerHTML='<div style="color:#dc2626;">Error loading users.</div>';return;}
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[60] Line 28728  (try starts line 28703)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if(!append)container.innerHTML='<div style="color:#dc2626;font-size:0.78rem;">Error loading audit log.</div>';
      }
    Try block does: var res=await window.sb.from('auth_log').select('*').order('created_at',{ascending:false}).range(_authAuditOff…  |  if(!res.data||!res.data.length){
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[61] Line 28952  (try starts line 28939)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var raw = localStorage.getItem('_summaryCache');  |  if (raw) {
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[62] Line 28967  (try starts line 28964)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: var c = JSON.parse(value);  |  if (c) _saveSummaryCache(c);
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[63] Line 28980  (try starts line 28977)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: var arr = JSON.parse(localStorage.getItem('inv_pending_queue') || '[]');  |  if (Array.isArray(arr)) count += arr.length;
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
[64] Line 28984  (try starts line 28981)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: var arr2 = JSON.parse(localStorage.getItem('inv_pending_updates') || '[]');  |  if (Array.isArray(arr2)) count += arr2.length;
    Risk: MEDIUM — try block reads/fetches data (select/fetch/JSON.parse/localStorage read) — a failure silently falls back to stale/missing data
════════════════════════════════════════════════════════════════════════
LOW RISK  (52 catch blocks)
════════════════════════════════════════════════════════════════════════

[1] Line 2394  (try starts line 2394)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        return s;
      }
    Try block does: return new Date(s).toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'});
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[2] Line 4805  (try starts line 4805)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: document.body.removeChild(iframe);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[3] Line 4808  (try starts line 4802)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: iwin.addEventListener('afterprint', function () {  |  setTimeout(function () {
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[4] Line 4815  (try starts line 4815)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: document.body.removeChild(iframe);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[5] Line 4818  (try starts line 4811)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: Object.defineProperty(iwin, 'close', {  |  configurable: true, writable: true,
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[6] Line 6432  (try starts line 6422)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _logoutSess = (typeof _authGetSession === 'function') ? _authGetSession() : null;  |  var _logoutUKey = _logoutSess && _logoutSess.id ? _logoutSess.id : null;
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[7] Line 6492  (try starts line 6486)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        err.textContent = '⚠️ Connection error. Check internet and try again.';
        err.style.display = 'block';
        btn.disabled = false;
        btn.textContent = '🔍 Find My Account';
      }
    Try block does: err.textContent = '❌ Account recovery is not available. Please contact admin to reset your password.';  |  err.style.display = 'block';
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[8] Line 6550  (try starts line 6547)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        err.textContent = '⚠️ Error saving password. Check connection and try again.';
        err.style.display = 'block';
        btn.disabled = false;
        btn.textContent = '✅ Save New Password & Login';
      }
    Try block does: suc.textContent = '❌ Password recovery is not available. Please contact admin.';  |  return;
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[9] Line 8255  (try starts line 8255)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('PDF library failed to load. Check your connection and try again.'); return;
      }
    Try block does: await _loadHtml2Pdf();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[10] Line 8293  (try starts line 8293)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        window.print(); return;
      }
    Try block does: await _loadHtml2Pdf();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[11] Line 8393  (try starts line 8393)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        toast('⚠ PDF library failed to load'); return;
      }
    Try block does: await _loadHtml2Pdf();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[12] Line 8435  (try starts line 8431)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var _fitSt = pw.document.createElement('style');  |  _fitSt.textContent = 'html,body{width:100% !important;max-width:100% !important;margin:0 !important;padding:0 …
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[13] Line 8437  (try starts line 8437)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var logoEl = pw.document.getElementById('printLogo'); if (logoEl && _logo2) { logoEl.src = _logo2; logoEl.styl…
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[14] Line 8507  (try starts line 8507)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: document.body.removeChild(_printDiv);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[15] Line 8508  (try starts line 8508)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: document.head.removeChild(_styleTag);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[16] Line 9829  (try starts line 9829)  —  var: _se
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_se) {}
    Try block does: localStorage.setItem('cash_holders_cache', JSON.stringify({names: names, entries2: entries2, entries3: entries…
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[17] Line 10077  (try starts line 10072)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: localStorage.setItem('summary_cache', JSON.stringify({  |  cashInHand: cashInHand, remaining: displayRemaining,
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[18] Line 10123  (try starts line 10123)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        synced = '—';
      }
    Try block does: synced = new Date(c.timestamp).toLocaleString('en-PK', {dateStyle:'short',timeStyle:'short'});
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[19] Line 10229  (try starts line 10229)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _invAllEntries = [];
      }
    Try block does: _invAllEntries = await fetchEntriesWithCorrectInvoiceAmounts({});
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[20] Line 10703  (try starts line 10700)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var img = pw.document.getElementById('printLogo');  |  if (img) { img.src = logo; img.style.display = 'block'; }
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[21] Line 10765  (try starts line 10765)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: pw.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[22] Line 11090  (try starts line 11090)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: w.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[23] Line 11099  (try starts line 11099)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        alert('Failed to load Excel library. Check internet connection.'); return;
      }
    Try block does: XLSX = await pbLoadXLSX();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[24] Line 11425  (try starts line 11423)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        targetEl.innerHTML = '<div style="padding:24px;text-align:center;color:#dc2626;font-family:Montserrat,sans-serif;">Error loading invoices: '+e.message+'</div>';
        return;
      }
    Try block does: allInvs = await fetchCustomerInvoices(name, phone, nameQuery);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[25] Line 12101  (try starts line 12101)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: pw.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[26] Line 13730  (try starts line 13719)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        cb(src);
      }
    Try block does: var MAX = 800;  |  if (img.width <= MAX && img.height <= MAX) { cb(src); return; }
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[27] Line 13734  (try starts line 13716)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        cb(src);
      }
    Try block does: var img = new Image();  |  img.onload = function() {
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[28] Line 13758  (try starts line 13754)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: localStorage.setItem('inv_logo', small);  |  if (typeof _ctrlLogo !== 'undefined' && _ctrlLogo === logo) _ctrlLogo = small;
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[29] Line 13826  (try starts line 13826)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: updateDailyCategories();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[30] Line 13827  (try starts line 13827)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: updateLedgerCategories();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[31] Line 13830  (try starts line 13830)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: renderDashboard();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[32] Line 13834  (try starts line 13834)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: renderLedger();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[33] Line 17272  (try starts line 17272)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: pw.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[34] Line 17516  (try starts line 17516)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: win.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[35] Line 17996  (try starts line 17996)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: printWin.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[36] Line 18652  (try starts line 18619)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if (amtInput) amtInput.value=0;
      }
    Try block does: const ym = date.slice(0,7);  |  const [_y,_m] = ym.split('-').map(Number);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[37] Line 18796  (try starts line 18763)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if (amtInput) amtInput.value=0;
      }
    Try block does: const ym = date.slice(0,7);  |  const [_y,_m] = ym.split('-').map(Number);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[38] Line 18966  (try starts line 18922)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if (_isStale()) return;
        if (amtInput) { amtInput.value = '0'; amtInput.dataset.rawvalue = '0'; }
        if (info) { info.style.display=''; info.textContent='❌ Error: '+(e.message||e); info.style.color='#dc2626'; }
      }
    Try block does: if (info) { info.style.display=''; info.textContent='⏳ Fetching from salary payments…'; info.style.color='#647…  |  const allDays = sDays(); // {empId|YYYY-MM: [{id,date,amount,ot_nights,ot_days,mess,room,note}]}
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[39] Line 20607  (try starts line 20607)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: pw.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[40] Line 21017  (try starts line 21015)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        if (tbody) tbody.innerHTML = '<tr><td colspan="6" style="text-align:center;padding:24px;color:#ef4444;">❌ Failed to load data</td></tr>';
        return;
      }
    Try block does: entries = await fetchEntriesWithCorrectInvoiceAmounts({});
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[41] Line 23323  (try starts line 23323)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: w.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[42] Line 24960  (try starts line 24960)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var pLogo=pw.document.getElementById('printLogo'); if(pLogo){pLogo.src=_stmtLogo;pLogo.style.display='block';}
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[43] Line 24962  (try starts line 24962)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: pw.print();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[44] Line 26381  (try starts line 26381)  —  var: ex
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(ex) {}
    Try block does: var pi=pw.document.getElementById('printLogo');if(pi)pi.src=_ctrlLogo;
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[45] Line 26926  (try starts line 26926)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        _mobPrintNative(); return;
      }
    Try block does: await _loadHtml2Pdf();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[46] Line 28918  (try starts line 28915)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: var el = document.getElementById('offlineBanner');  |  if (el) el.style.display = 'none';
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[47] Line 29037  (try starts line 29031)  —  var: e
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(e) {}
    Try block does: if (tab === 'summary' || tab === 'finance') setTimeout(_updatePendingNotice, 200);  |  if (tab === 'summary' && window._isOfflineSession) {
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[48] Line 29047  (try starts line 29047)  —  var: e
    Pattern: NEAR-EMPTY — does something (toast/UI/fallback) but never logs the actual error object
    Catch block:
      catch(e) {
        throw e;
      }
    Try block does: r = await _orig.apply(this, arguments);
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[49] Line 29048  (try starts line 29048)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: _updatePendingNotice();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[50] Line 29078  (try starts line 29078)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: _updatePendingNotice();
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[51] Line 29085  (try starts line 29079)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: if (el) {  |  el.style.background = '#065f46';
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
[52] Line 29096  (try starts line 29087)  —  var: _
    Pattern: COMPLETELY EMPTY — catch(x){}
    Catch block:
      catch(_) {}
    Try block does: _hideOfflineBanner();  |  window._isOfflineSession = false;
    Risk: LOW — try block only touches the DOM/UI/local formatting — lowest impact if swallowed
════════════════════════════════════════════════════════════════════════
SUMMARY
════════════════════════════════════════════════════════════════════════

156 of 330 catch blocks (47%) in index.html swallow the actual JS Error
object without ever recording it anywhere a developer could see it later —
not in the console, not in Sentry, not in any log table. The app already
uses Sentry.captureException(e) as an error-reporting pattern in 8 OTHER
catch blocks elsewhere in the file (all of which also log to console), so
the pattern is known and available — it is simply not applied in any of
these 156. 40 of the 156 sit directly on top of a Supabase write
(insert/update/upsert/delete/rpc), meaning a failed save can currently fail
completely silently — the UI may show a generic success toast, or nothing
at all, while the actual write never happened.

NO CODE CHANGES WERE MADE — this is a read-only audit.
